const API_BASE = "/"; // 기본 URL

const ENDPOINTS = {
  GET_CARD_DATA: `/get/cards`,
  //ADD_ITEM: `/add`,
  //DELETE_ITEM: (id) => `/delete/${id}`, // 동적인 값이 필요한 경우 함수 사용
};

export default ENDPOINTS;
